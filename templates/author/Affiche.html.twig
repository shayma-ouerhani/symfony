<body>
<h1>List authors</h1>
<table border="3">
<tr>
    <th>Username</th>
    <th>Email</th>
</tr>
    <tr>
        <tr>{% for author in author %} </tr>
        <td>{{author.username}}</td>
        <td>{{author.email}}</td>
        <td><a href="{{path('editauthor',{'id':author.id})}}">edit</a></td>
         {% endfor %}
    </tr>
</table>
</body>