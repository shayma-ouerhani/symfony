<form method="post" action="{{ path('app_studentt_delete', {'id': studentt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ studentt.id) }}">
    <button class="btn">Delete</button>
</form>
