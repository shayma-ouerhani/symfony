{% extends 'base.html.twig' %}

{% block title %}Studentt{% endblock %}

{% block body %}
    <h1>Studentt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ studentt.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ studentt.name }}</td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ studentt.cin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_studentt_index') }}">back to list</a>

    <a href="{{ path('app_studentt_edit', {'id': studentt.id}) }}">edit</a>

    {{ include('studentt/_delete_form.html.twig') }}
{% endblock %}
